name: Check
on: push
jobs:
  init:
    runs-on: ubuntu-latest
    steps:
      - uses: Checkout
      - uses: actions/checkout@v3
      - name: Install deps
        run: npm ci
      - name: Test
        run: npm run test        
      